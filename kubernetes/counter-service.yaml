apiVersion: v1
kind: Service
metadata:
  name: counter-service
  namespace: yann-pomie
spec:
  selector:
    app: counter
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
